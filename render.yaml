services:
  - type: web
    name: allury-stockflow
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ifood_server:app --host 0.0.0.0 --port $PORT
    disk:
      name: dados
      mountPath: /opt/render/project/src/data
      sizeGB: 1
